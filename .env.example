# Example environment variables for local development
# Copy to .env (do NOT commit .env with your secret keys)

# VAPID keys for web-push (generate via `node server/generateVapid.js`)
VAPID_PUBLIC=
VAPID_PRIVATE=

# URL of your push server (e.g., Render or localhost during dev)
PUSH_SERVER_URL=http://localhost:3000
